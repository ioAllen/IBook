<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moduleBookshelf">

    <application
        android:name="com.common.base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/bookshelf_app_name"
        android:supportsRtl="true"
        android:theme="@style/public_AppTheme">

        <activity android:name="com.moduleBookshelf.ui.BookshelfLaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.BookshelfMainActivity"
            android:screenOrientation="portrait" />
    </application>
</manifest>
