apply from:"../common_component_build.gradle"

android {
    resourcePrefix "common_" //给 Module 内的资源名增加前缀, 避免资源名冲突
}

dependencies {
    compile "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    implementation project(":lib_common")
}
repositories {
    mavenCentral()
}
