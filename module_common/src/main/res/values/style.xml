<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="common_RatingStyle" parent="@android:style/Widget.RatingBar">
        <!-- 定义星星图片 -->
        <item name="android:progressDrawable">@drawable/common_star_conf_file1</item>
        <!-- 根据自定义星星图片的大小,设置相应的值,否则可能显示不全 -->
        <item name="android:minHeight">@dimen/x26</item>
        <item name="android:maxHeight">@dimen/x26</item>
    </style>
</resources>